/omnitoken
*.db
*.db-shm
*.db-wal
/dist/

# machine-local, not project config
.DS_Store
.claude/settings.local.json
# Scratch git worktrees for parallel subagents; each is its own checkout.
.claude/worktrees/
